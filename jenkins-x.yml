pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: busybox
        stages:
          - name: A Working Stage
            options:
              retry: 4
            steps:
              - command: exit 1
